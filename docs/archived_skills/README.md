# Archived skills (not agent-discoverable)

Moved out of `.grok/skills/` so Grok/Cursor do **not** inject full skill cards every turn.

- `grok/` — former `.grok/skills/_archive` design/graphify packs
- `claude/` — former `.claude/skills/_archive` + demoted mirrors
- `optional/` — demoted active skills (agent-routing, token-discipline, etc.)

**Do not open** unless the user names a specific skill. Prefer `AGENTS.md` + `python dev.py route-task`.
